//Loop through an array and print each item to the console
let inventory = ["Sword", "Shield", "Potion", "Bow"];

for(let i = 0; i < inventory.length; i++) {
    console.log(inventory[i]);
}

//bonus
for(let i = 0; i < inventory.length; i++) {
    console.log(i + " " + inventory[i]);
}